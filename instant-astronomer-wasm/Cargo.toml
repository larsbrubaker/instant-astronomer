[package]
name = "instant-astronomer-wasm"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "WebAssembly (wasm-bindgen) shell for Instant-Astronomer."

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
instant-astronomer-core = { path = "../instant-astronomer-core" }
agg-gui = { workspace = true }
# The published renderer `web_shell` paints through.
agg-gui-wgpu = { workspace = true }
# Repo-only shim, kept solely for `web_shell` (canvas + rAF loop + DOM
# input) — there is no published browser shell crate yet.
demo-wgpu = { workspace = true }

[target.'cfg(target_arch = "wasm32")'.dependencies]
wasm-bindgen = "0.2"
js-sys = "0.3"
web-sys = { version = "0.3", features = [
    "Window",
    "Navigator",
    "Geolocation",
    "Position",
    "PositionError",
    "PositionOptions",
    "Coordinates",
    "console",
] }
